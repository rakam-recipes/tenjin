version: 2

sources:
  - name: tenjin
    tables:
      - name: campaign_buckets
        description: List of campaign buckets
        meta:
          rakam:
            hidden: true
            category: Marketing
        columns:
          - name: name
            meta:
              rakam.dimension:
                type: string
