version: 2

sources:
  - name: marketing
    tables:
    - name: countries
      meta:
        hidden: true
      columns:
        - name: name
          meta:
            rakam.dimension:
              type: string
        - name: code
          meta:
            rakam.dimension:
              type: string
